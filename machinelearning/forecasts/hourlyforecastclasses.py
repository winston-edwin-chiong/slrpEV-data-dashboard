from sklearn.base import TransformerMixin, BaseEstimator

def HourlyForecast(BaseEstimator, TransformerMixin):
    
    def fit(self, X, y=None):
        return X
    
    def transform(self, X, y=None):
        return 
    
    
